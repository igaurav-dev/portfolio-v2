Drop `screenshot.png` here — the main README references it at the top.

A 2600×1200 capture of the home page in dark mode works well: the nav, the hero,
and the trace strip pinned to the bottom all in one frame.
